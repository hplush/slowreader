{
  "name": "@slowreader/server",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./package.json": "./package.json",
    "./test": "./test.ts"
  },
  "scripts": {
    "test": "FORCE_COLOR=1 pnpm run /^test:/",
    "start": "node --watch index.ts --port 2554",
    "migration": "drizzle-kit generate && oxfmt ./db/**/*.json",
    "database": "drizzle-kit studio --host 0.0.0.0 --port 2557",
    "build": "node --run clean:build && pnpm run /^build:/",
    "production": "node --run build && ./scripts/run-image.sh",
    "build:server": "pnpm -F server --prod deploy dist",
    "build:assets": "mkdir -p web/ && cp -fr ../web/dist web/ && cp ../web/routes.regexp web/",
    "test:server-coverage": "bnt --coverage 100 --coverage-exclude '{lib/exit.ts,db/index.ts,../api/**,../proxy/**,test.ts}'",
    "clean:build": "rm -rf dist/ web/"
  },
  "dependencies": {
    "@electric-sql/pglite": "0.4.4",
    "@logux/actions": "0.5.0",
    "@logux/server": "0.14.0",
    "@slowreader/api": "workspace:*",
    "@slowreader/proxy": "workspace:*",
    "argon2": "0.44.0",
    "cookie": "1.1.1",
    "drizzle-orm": "0.45.2",
    "nanoid": "5.1.9",
    "postgres": "3.4.9",
    "tsx": "4.21.0"
  },
  "devDependencies": {
    "@logux/client": "0.23.2",
    "better-node-test": "0.8.4",
    "drizzle-kit": "0.31.10"
  },
  "engines": {
    "node": "^24.15.0",
    "pnpm": "^10.0.0"
  }
}
